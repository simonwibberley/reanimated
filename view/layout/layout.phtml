<?php

// As the stylesheet stack is empty by default, the use of append vs. prepend is
// irrelevant in this context.
$this->headLink()->appendStylesheet($this->assetUrl('css/style.css'));
$this->headLink(['rel' => 'icon', 'href' => $this->assetUrl('img/favicon.png')]);

$this->headScript()->prependFile($this->assetUrl('js/reanimated.js'));
?>

<?= $this->doctype() ?>

<html>
  <head>
    <?= $this->headLink() ?>
    <?= $this->headScript() ?>
  </head>

  <body>

    <?= $site->publicNav()->menu() ?>

  <?= $this->content ?>
  </body>
</html>
